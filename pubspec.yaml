name: certificazioni_elettriche
description: App per la compilazione guidata di documentazione per impianti elettrici
publish_to: "none"
version: 0.8.0+8

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  pdf: ^3.11.3
  printing: ^5.14.2
  file_picker: ^10.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
